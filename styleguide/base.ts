const margin = {
  margin: {
    margin: "1rem"
  },
  marginLeft: {
    "margin-left": "1rem"
  }
};

const padding = {
  padding: {
    padding: "1rem"
  },
  paddingLeft: {
    "padding-left": "1rem"
  }
};

const dimension = {
  dimension: {
    width: "100px",
    height: "100px"
  }
};

const bgColor = {
  bgColor: {
    "background-color": "red"
  }
};

const radius = {
  radius: {
    "border-radius": "50%"
  }
};

export { margin, padding, dimension, bgColor, radius };
